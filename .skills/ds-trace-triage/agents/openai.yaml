interface:
  display_name: "DataSystem Trace Triage"
  short_description: "分析慢时延、关键瓶颈 TopN 和同 Worker 时间关联"
  default_prompt: "Use $ds-trace-triage to analyze DataSystem traces and generate a source-backed main-bottleneck TopN report with same-Worker QueryMeta, RemoteGet, RPC, and URMA correlation; obtain local_cache mode before assigning topology."
