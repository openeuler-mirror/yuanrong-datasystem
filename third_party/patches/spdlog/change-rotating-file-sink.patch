diff -ruN a/include/spdlog/sinks/rotating_file_sink-inl.h b/include/spdlog/sinks/rotating_file_sink-inl.h
--- a/include/spdlog/sinks/rotating_file_sink-inl.h	2026-05-01 01:01:26.663599812 +0800
+++ b/include/spdlog/sinks/rotating_file_sink-inl.h	2026-05-01 01:01:26.667599778 +0800
@@ -16,7 +16,13 @@
+#include <algorithm>
 #include <cerrno>
 #include <chrono>
+#include <cstdio>
 #include <ctime>
+#include <dirent.h>
+#include <iomanip>
 #include <mutex>
+#include <sstream>
 #include <string>
 #include <tuple>
+#include <vector>
 
@@ -64,6 +70,22 @@
     return fmt_lib::format(SPDLOG_FILENAME_T("{}.{}{}"), basename, index, ext);
 }
 
+// calc filename according to timestamp (with milliseconds) and file extension if exists.
+// e.g. calc_filename("logs/mylog.txt", 2024-10-21 15:30:45, 123) => "logs/mylog.20241021153045_123.txt".
+template <typename Mutex>
+SPDLOG_INLINE filename_t rotating_file_sink<Mutex>::calc_filename(const filename_t &filename,
+                                                                  std::tm time_tm,
+                                                                  int millis) {
+    filename_t basename;
+    filename_t ext;
+    std::tie(basename, ext) = details::file_helper::split_by_extension(filename);
+
+    std::ostringstream oss;
+    oss << basename << "." << std::put_time(&time_tm, "%Y%m%d%H%M%S")
+        << "_" << std::setfill('0') << std::setw(3) << millis << ext;
+    return oss.str();
+}
+
 template<typename Mutex>
 SPDLOG_INLINE filename_t rotating_file_sink<Mutex>::filename()
 {
@@ -101,41 +123,131 @@
 }
 
 // Rotate files:
-// log.txt -> log.1.txt
-// log.1.txt -> log.2.txt
-// log.2.txt -> log.3.txt
-// log.3.txt -> delete
-template<typename Mutex>
-SPDLOG_INLINE void rotating_file_sink<Mutex>::rotate_()
-{
+// log.txt -> log.YYYYMMDDHHMMSS_mmm.txt  (e.g., log.20251020143022_123.txt)
+template <typename Mutex>
+SPDLOG_INLINE void rotating_file_sink<Mutex>::rotate_() {
     using details::os::filename_to_str;
-    using details::os::path_exists;
 
     file_helper_.close();
-    for (auto i = max_files_; i > 0; --i)
-    {
-        filename_t src = calc_filename(base_filename_, i - 1);
-        if (!path_exists(src))
-        {
-            continue;
-        }
-        filename_t target = calc_filename(base_filename_, i);
 
-        if (!rename_file_(src, target))
-        {
-            // if failed try again after a small delay.
-            // this is a workaround to a windows issue, where very high rotation
-            // rates can cause the rename to fail with permission denied (because of antivirus?).
+    auto now = std::chrono::system_clock::now();
+    auto now_time_t = std::chrono::system_clock::to_time_t(now);
+    auto tm_time = details::os::localtime(now_time_t);
+    auto ms = std::chrono::duration_cast<std::chrono::milliseconds>(
+                  now.time_since_epoch()) % 1000;
+
+    filename_t archive_filename = calc_filename(base_filename_, tm_time, static_cast<int>(ms.count()));
+
+    if (!rename_file_(base_filename_, archive_filename)) {
+        // Fallback: try appending counter suffix to avoid filename collision.
+        bool renamed = false;
+        for (int i = 1; i <= 10; ++i) {
+            filename_t fb_basename;
+            filename_t fb_ext;
+            std::tie(fb_basename, fb_ext) =
+                details::file_helper::split_by_extension(archive_filename);
+            std::ostringstream oss;
+            oss << fb_basename << "_" << i << fb_ext;
+            if (rename_file_(base_filename_, oss.str())) {
+                renamed = true;
+                break;
+            }
+        }
+        if (!renamed) {
+            // Last resort: sleep and retry once.
             details::os::sleep_for_millis(100);
-            if (!rename_file_(src, target))
-            {
-                file_helper_.reopen(true); // truncate the log file anyway to prevent it to grow beyond its limit!
+            if (!rename_file_(base_filename_, archive_filename)) {
+                file_helper_.reopen(true);
                 current_size_ = 0;
-                throw_spdlog_ex("rotating_file_sink: failed renaming " + filename_to_str(src) + " to " + filename_to_str(target), errno);
+                throw_spdlog_ex(
+                    "rotating_file_sink: failed renaming " +
+                        filename_to_str(base_filename_) + " to " +
+                        filename_to_str(archive_filename),
+                    errno);
             }
         }
     }
+
     file_helper_.reopen(true);
+    current_size_ = 0;
+
+    if (max_files_ == 0)
+    {
+        return;
+    }
+
+    const auto max_archives = max_files_ - 1;
+    filename_t basename;
+    filename_t ext;
+    std::tie(basename, ext) = details::file_helper::split_by_extension(base_filename_);
+    const auto slash_pos = basename.find_last_of(SPDLOG_FILENAME_T("/\\"));
+    const filename_t dir = slash_pos == filename_t::npos ? SPDLOG_FILENAME_T(".") : basename.substr(0, slash_pos);
+    const filename_t stem = slash_pos == filename_t::npos ? basename : basename.substr(slash_pos + 1);
+    const filename_t prefix = stem + SPDLOG_FILENAME_T(".");
+
+    auto is_digits = [](const filename_t &text, std::size_t begin, std::size_t end) {
+        for (std::size_t i = begin; i < end; ++i)
+        {
+            if (text[i] < '0' || text[i] > '9')
+            {
+                return false;
+            }
+        }
+        return true;
+    };
+
+    auto is_rotated_archive = [&](const filename_t &name) {
+        if (name.size() <= prefix.size() + ext.size() || name.compare(0, prefix.size(), prefix) != 0)
+        {
+            return false;
+        }
+        if (!ext.empty() && name.compare(name.size() - ext.size(), ext.size(), ext) != 0)
+        {
+            return false;
+        }
+
+        const auto payload_size = name.size() - prefix.size() - ext.size();
+        const filename_t payload = name.substr(prefix.size(), payload_size);
+        // Match YYYYMMDDHHMMSS_mmm and the rare fallback YYYYMMDDHHMMSS_mmm_N.
+        if (payload.size() != 18 && !(payload.size() > 19 && payload[18] == '_'))
+        {
+            return false;
+        }
+        if (!is_digits(payload, 0, 14) || payload[14] != '_' || !is_digits(payload, 15, 18))
+        {
+            return false;
+        }
+        return payload.size() == 18 || is_digits(payload, 19, payload.size());
+    };
+
+    std::vector<filename_t> archives;
+    DIR *dir_handle = opendir(dir.c_str());
+    if (dir_handle == nullptr)
+    {
+        return;
+    }
+    struct dirent *entry = nullptr;
+    while ((entry = readdir(dir_handle)) != nullptr)
+    {
+        filename_t name(entry->d_name);
+        if (is_rotated_archive(name))
+        {
+            archives.push_back(name);
+        }
+    }
+    closedir(dir_handle);
+
+    if (archives.size() <= max_archives)
+    {
+        return;
+    }
+    std::sort(archives.begin(), archives.end());
+    const auto prune_count = archives.size() - max_archives;
+    for (std::size_t i = 0; i < prune_count; ++i)
+    {
+        const filename_t path = dir + SPDLOG_FILENAME_T("/") + archives[i];
+        (void)std::remove(path.c_str());
+    }
 }
 
 // delete the target if exists, and rename the src file  to target
diff -ruN a/include/spdlog/sinks/rotating_file_sink.h b/include/spdlog/sinks/rotating_file_sink.h
--- a/include/spdlog/sinks/rotating_file_sink.h	2026-05-01 01:01:26.663599812 +0800
+++ b/include/spdlog/sinks/rotating_file_sink.h	2026-05-01 01:01:26.667599778 +0800
@@ -8,6 +8,7 @@
 #include <spdlog/details/null_mutex.h>
 #include <spdlog/details/synchronous_factory.h>
 
+#include <ctime>
 #include <chrono>
 #include <mutex>
 #include <string>
@@ -25,6 +26,7 @@
     rotating_file_sink(filename_t base_filename, std::size_t max_size, std::size_t max_files, bool rotate_on_open = false,
         const file_event_handlers &event_handlers = {});
     static filename_t calc_filename(const filename_t &filename, std::size_t index);
+    static filename_t calc_filename(const filename_t &filename, std::tm time_tm, int millis);
     filename_t filename();
 
 protected:
@@ -33,10 +35,7 @@
 
 private:
     // Rotate files:
-    // log.txt -> log.1.txt
-    // log.1.txt -> log.2.txt
-    // log.2.txt -> log.3.txt
-    // log.3.txt -> delete
+    // log.txt -> log.YYYYMMDDHHMMSS_mmm.txt  (e.g., log.20251020143022_123.txt)
     void rotate_();
 
     // delete the target if exists, and rename the src file  to target
