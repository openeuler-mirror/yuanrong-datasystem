# tests/st/common/metrics — histogram P99 perf (ds_cc_test; no //tests/st:st_common).
load("//bazel:build_defs.bzl", "ds_cc_test")

package(default_visibility = ["//visibility:public"])

ds_cc_test(
    name = "histogram_p99_perf_test",
    srcs = ["histogram_p99_perf_test.cpp"],
    size = "large",
    timeout = "long",
    copts = [
        '-DLLT_BIN_PATH=\\"/tmp/ds_llt\\"',
    ],
    deps = [
        "//src/datasystem/common/flags:ds_flags",
        "//src/datasystem/common/log:common_log",
        "//src/datasystem/common/metrics:common_metrics",
        "//src/datasystem/common/util:file_util",
    ],
)
