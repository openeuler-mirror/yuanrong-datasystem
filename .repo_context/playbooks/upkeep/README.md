# Upkeep Playbooks

`playbooks/upkeep/` contains workflows for maintaining `.repo_context/` itself.

Use this directory when the task is to generate, backfill, split, or formalize repository context rather than to
implement a product feature or triage a runtime issue.

Current docs:

- `module-context-generation.md`
