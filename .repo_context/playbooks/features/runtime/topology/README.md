# Runtime Topology Playbooks

Use this directory for feature or bugfix workflows that change Phase2 runtime topology, including worker membership,
routing, placement, topology repository schema, topology tasks, startup recovery, and scale transitions.

Current docs:

- `implementation.md`: implementation and review workflow for topology changes.
