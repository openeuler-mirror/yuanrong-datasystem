# Quality Feature Playbooks

Use this directory for recurring quality-layer implementation workflows.

Current docs:

- `cmake-build-optimization.md`: optimize or refactor CMake build targets, dependencies, third-party rules, and
  multi-language package outputs.
- `test-implementation.md`: choose, add, label, and validate tests in the current `tests` tree.
