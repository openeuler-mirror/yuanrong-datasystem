# Logging Feature Playbooks

`playbooks/features/infra/logging/` contains implementation workflows for logging-related features.

Use this directory when a change touches `Trace`, `AccessRecorder`, `Logging`, `LogManager`, failure handling, or exporter behavior that is part of logging feature work.

Current docs:

- `implementation.md`
