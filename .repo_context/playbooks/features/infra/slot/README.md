# Slot Feature Playbooks

`playbooks/features/infra/slot/` contains implementation workflows for distributed-disk slot storage, takeover, and
recovery features.

Use this directory when a change touches slot manifest or index semantics, compaction, takeover, preload, restart
recovery, failed-worker recovery, or slot-specific DFX behavior.

Current docs:

- `implementation.md`
